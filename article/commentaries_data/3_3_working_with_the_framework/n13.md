+++
id = "n13"
section = "3.3. Working with the framework"
category = "alignment"
category_label = "Alignment / RLHF"
marker = "⁂"
title = "The reward-model dataset already exists — it's being discarded"
filenote = "article/analysis/ouyang_2022_instructgpt.md"
tagline = "Comparative inference · speculative extension"
anchor_preview = "This three-level rating, over multiple sampled candidates, is already two of the three ingredients a reward-model dataset needs."

[[sources]]
text = "Training LMs to Follow Instructions (InstructGPT) — arXiv:2203.02155"
url = "https://arxiv.org/abs/2203.02155"
verified = true
+++
InstructGPT's core finding is that a model bigger at pretraining but never aligned to instructions loses to a much smaller aligned one — a near match for DeepSeek-V3.1 (671B) topping out at 27% compliance while Ouyang et al.'s 1.3B aligned model beat 175B raw GPT-3. Two of the three ingredients their reward-model training needs already exist here: multiple candidate loss edits per comment (from T=1.0 sampling) and a three-level expert verdict that is a ready-made ordinal preference signal. The only missing step is saving every candidate and its verdict instead of keeping one and discarding the rest — turning 20-launch prototyping data into training data for the fine-tuned generator the paper doesn't yet have.

## Sources & Evidence

This section maps each claim to verbatim citations from local transcriptions.

1) Claim: "RLHF constructs a reward-model dataset from ranked multiple candidates; RM trained on pairwise preferences; then PPO uses RM as reward."
   - Evidence: Ouyang et al. (2022), Training language models to follow instructions with human feedback (NeurIPS 2022).
   - Quotes (verbatim):
     > "We then collect a dataset of rankings of model outputs, which we use to further fine-tune this supervised model using reinforcement learning from human feedback. We call the resulting models InstructGPT." (Abstract)

     > "We then collect a dataset of human-labeled comparisons between outputs from our models on a larger set of API prompts. We then train a reward model (RM) on this dataset to predict which model output our labelers would prefer. Finally, we use this RM as a reward function and fine-tune our supervised learning baseline to maximize this reward using the PPO algorithm." (§1 / Method overview)

     > "Step 2 Collect comparison data, and train a reward model… A prompt and several model outputs are sampled… A labeler ranks the outputs from best to worst… This data is used to train our reward model." (Figure 2, Step 2 description)

     > "The loss function for the RM becomes: loss(θ) = - 1/\(K choose 2) E[ log σ(r_θ(x,y_w) − r_θ(x,y_l)) ]" (§3.4, Eq. (1))

2) Claim: "Test-time Best‑of‑N (arena selection) from multiple candidates improves outcomes; generate multiple candidate reasoning traces, then select by a learned evaluator."
   - Evidence: Zhang et al. (2026), Characterizing, Evaluating, and Optimizing Complex Reasoning (TRM / ME^2).
   - Quotes (verbatim):
     > "We introduce the ME^2 principle … construct the TRM-Preference dataset and train a Thinking Reward Model (TRM)… At test time, selecting better reasoning leads to better outcomes (up to 19.3% gain), and during RL training, thinking rewards enhance reasoning and performance." (Abstract)

     > "We conduct comprehensive experiments… At test time, TRM enables effective test-time scaling by selecting high-quality reasoning traces, yielding gains of up to 19.3%." (§1 / contributions)

     > "For each prompt, we generate multiple candidate reasoning traces and apply the evaluation pipeline…" (§ dataset construction)

Figures/Tables to cite (no images embedded):
- Ouyang 2022: Figure 2 (pipeline SFT → RM → PPO); Eq. (1) (RM pairwise loss).
- Zhang 2026: Figure 1 (framework panels incl. Best‑of‑N selection); sections on dataset construction and gains (up to 19.3%).
