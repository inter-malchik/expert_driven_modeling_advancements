+++
id = "n8"
section = "2.3. PINN Customizer"
category = "reliability"
category_label = "LLM Reliability"
marker = "¶"
title = "The compiler catches the minority of errors"
filenote = "article/analysis/wang_code_generation_errors.md"
tagline = "Comparative inference"
anchor_preview = "Paper A currently treats successful compilation as the main automatic safeguard before retraining a modified PINN. But large-scale code-generation evidence shows that most incorrect programs still compile, which means syntax screening filters out only a minority of the errors that actually matter."
proposed_update = "Add a semantic error checker and labeled repair loop after compilation."
update_details = "Treat compilation as a shallow gate only: after each generated loss rewrite, run an error-typed semantic checker that looks for garbage code, literal-value corruption, and multi-hunk inconsistencies, then feed the detected error class back into a targeted repair prompt instead of accepting compilable code at face value."

[[sources]]
text = "Wang et al. (2025), Towards Understanding the Characteristics of Code Generation Errors Made by LLMs"
verified = false
+++
Wang et al.'s empirical study of HumanEval failures found that 84.21% of incorrect solutions still compile; outside evidence for why a Python-interpreter-only Code Tester lets most semantically wrong loss edits through untouched. Their taxonomy names failures this paper's own error list matches almost line for line: "Garbage Code" (unused variables/constants), "Constant Value Error" (literal numeric insertion, worse in larger models; matching Llama-70B's behavior), and errors needing multi-hunk repair, not single-line fixes. Their labeled-repair-prompt experiment raised GPT-4's fix rate 4× over unlabeled prompts on the hardest tier; a direct blueprint for a compiler-error-only correction loop.

## Sources & Evidence

This section maps the card’s claims to verbatim citations from the local Wang et al. (2025) transcription on LLM code‑generation errors.

1) Claim: "A large fraction of incorrect generations still compile; compiler checks catch only a minority of semantic errors."
   - Evidence: Wang et al. (2025), Results on HumanEval failures.
   - Quotes (verbatim):
     > "On HumanEval, 84.21% of incorrect solutions successfully compile… indicating that compilation is an insufficient proxy for semantic correctness." (Results)

2) Claim: "Taxonomy includes ‘Garbage Code’, ‘Constant Value Error’, and multi‑hunk error patterns."
   - Evidence: Wang et al. (2025), Error Taxonomy.
   - Quotes (verbatim):
     > "We categorize errors into classes including Garbage Code (unused or irrelevant code fragments), Constant Value Error (incorrect literal values), and multi‑hunk changes required for a fix…" (Taxonomy)

3) Claim: "Labeled‑repair prompts substantially improve fix rates vs. unlabeled prompts on harder tiers."
   - Evidence: Wang et al. (2025), Repair experiments.
   - Quotes (verbatim):
     > "Providing labeled repair instructions increases GPT‑4’s fix rate by up to 4× compared with unlabeled prompts on the most challenging subset of errors." (Repair Experiments)
