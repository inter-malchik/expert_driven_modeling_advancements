+++
id = "n30"
section = "3.4. Framework performance"
category = "optimization"
category_label = "Performance Diagnostics"
marker = "Ω"
title = "SBS vs VBS: Paper A's 27% is the SBS, not the ceiling"
filenote = "article/analysis/kerschke_2019_algorithm_selection_survey.md"
tagline = "Comparative inference"
anchor_preview = "The Meta-Llama-3.1-8B-Instruct model demonstrated lower compliance with comments (Figure 5)."

[[sources]]
text = "Kerschke et al. (2019), Automated Algorithm Selection: Survey and Perspectives"
url = "https://doi.org/10.1162/evco_a_00242"
verified = true
+++
Gindullina et al. (hereafter **Paper A**) evaluate three LLMs (Llama-3.1-8B, Llama-3.3-70B, DeepSeek-V3.1) across the same 20 comments and report per-LLM aggregate compliance rates of 6% / 25% / 27%. The paper's implicit conclusion — «DeepSeek-V3.1 is best» at 27% — treats LLM selection as a per-set decision: pick one algorithm and apply it to all instances. No per-comment analysis, no attempt to route different comment classes to different LLMs, no measurement of what the best-per-comment (oracle) LLM would achieve, and no discussion of instance features that could support per-comment routing — even though Paper A's own class/subclass ontology (Appendix A) is exactly such a feature set.

## Sources & Evidence

This section maps the card’s claims to verbatim citations (English) from the local transcription: “2019 Automated Algorithm Selection: Survey and Perspectives.md” (Kerschke et al., 2019).

1) Claim: "Per-instance algorithm selection exploits performance complementarity by choosing, for each instance, the algorithm expected to perform best."
   - Evidence: Kerschke et al. (2019), Abstract; §1 Introduction (lines 22–24, 32–36)
   - Quotes (verbatim):
     > "This performance complementarity can be exploited in various ways, one of which is based on the idea of selecting, from a set of given algorithms, for each problem instance to be solved the one expected to perform best. The task of automatically selecting an algorithm from a given set is known as the per-instance algorithm selection problem…"

2) Claim: "The per‑instance selection problem is defined formally; informative and cheaply computable instance features are key."
   - Evidence: §2 Algorithm Selection and Related Problems (lines 57–60)
   - Quotes (verbatim):
     > "Given a set I of instances of a problem P, a set A of algorithms … construct a selector S that maps any problem instance i ∈ I to an algorithm S(i) ∈ A such that the overall performance of S on I is optimal… This is typically achieved by making use of informative and cheaply computable features f(i). The features are of key importance…"

3) Claim: "Oracle/virtual best (VBS) provides a lower bound; single best solver (SBS) is the per‑set baseline; the VBS–SBS gap quantifies headroom."
   - Evidence: §2 (lines 61–63)
   - Quotes (verbatim):
     > "the performance of a (hypothetical) perfect per‑instance algorithm selector, often also referred to as an oracle selector or virtual best solver (VBS), provides a lower bound…"

     > "Another useful concept is that of the single best solver (SBS)… The difference or ratio between the performance of the SBS and VBS, also known as the VBS‑SBS gap, gives an indication of the performance gains that can be realised…"

4) Claim: "Large VBS–SBS gaps arise with strong performance complementarity; instance features enable selectors to close that gap."
   - Evidence: §2 (line 63)
   - Quotes (verbatim):
     > "It is important to note that the VBS‑SBS gap is large when the given set A of algorithms shows high performance complementarity on instance set I … Generally, per‑instance algorithm selection can be expected to achieve large performance gains … if … there is a set of sufficiently cheaply computable and informative instance features…"
