+++
id = "n33"
section = "4. Limitations and Future work"
category = "architecture"
category_label = "Multi-Agent Loops"
marker = "‡"
title = "Aglobe multi-agent architecture as blueprint"
filenote = "article/analysis/malek_2009_multi_agent_collaboration.md"
tagline = "Comparative inference"
anchor_preview = "Paper A currently chains one conversational agent, one classifier, one customizer, and one code-generating LLM, but its own future work already sketches evaluator and semantic-checker roles. The missing piece is not the idea of specialization, but an explicit coordination protocol between those roles."
proposed_update = "Split the pipeline into explicit evaluator, generator, checker, and coordinator agents."
update_details = "Turn the implicit future-work sketch into a concrete protocol: one agent proposes the loss edit, one scores compliance with the expert comment, one checks semantic alignment with modification rules, and a coordinator decides whether to accept, regenerate, or request clarification before retraining."

[[sources]]
text = "Malek (2009), Global Optimization through Meta-Heuristic Collaboration in a Multi-Agent System"
url = "https://www.seage.org/data/malek09specstudy.pdf"
verified = true
+++
Gindullina et al. (hereafter **Paper A**) implement a four-component pipeline: Conversational Agent → Hierarchical BERT classifier → PINN Customizer → off-the-shelf LLM (Llama-3.1-8B / Llama-3.3-70B / DeepSeek-V3.1). Compliance rates across the three LLMs are 6% / 25% / 27%, an implicit portfolio in which different LLMs perform differently on the same 20 comments. The paper's Future-work section names an explicit multi-agent extension: «*A promising direction is to evolve the system toward a multi-agent architecture, where specialized LLMs enhance autonomy and objectivity. An LLM-expert-evaluator could quantify forecast compliance ... An LLM-loss-semantic-checker could verify alignment between the generated code and modification rules.*» No architecture, no coordination protocol, no empirical demonstration.

## Sources & Evidence

This section maps the card's claims to verbatim citations (English) from the local transcription: `2009 Global Optimization through Meta-Heuristic Collaboration in a Multi-Agent System.md`.

1) Claim: "A multi-agent architecture can run several search methods simultaneously and let them share candidate solutions through a common pool."
   - Evidence: Malek (2009), overview of the approach.
   - Quotes (verbatim):
     > "The basic idea of our approach is to solve an optimization problem by various meta-heuristic algorithms simultaneously"

     > "Each meta-heuristic modifies one or many candidate solutions and shares them with other meta-heuristics through a global pool of candidate solutions."

     > "Each meta-heuristic and the solution pool are represented by their own agents which can work simultaneously and send messages to each other."

2) Claim: "The architecture separates explicit agent roles instead of leaving coordination implicit."
   - Evidence: Malek (2009), agent types / agent responsibilities.
   - Quotes (verbatim):
     > "There are following agent types in our system: a problem agent, a solution pool agent, an algorithm agent and an adviser agent."

     > "SolutionPool agent manages the population of candidate solutions and provides solutions to all algorithm agents."

     > "Algorithm agent which disposes of a particular meta-heuristic algorithm. It is responsible for: obtaining the algorithm parameter settings from the adviser agent ... running the meta-heuristic algorithm ... sending a report on previous algorithm run to the adviser agent"

3) Claim: "The collaboration protocol is operationalized through explicit message flow, initialization, repeated coordination cycles, and stop conditions."
   - Evidence: Malek (2009), system flow and agent messaging.
   - Quotes (verbatim):
     > "The agent initializes all other agents by sending the `init` message."

     > "After finishing, the algorithm agent sends the best solution to the problem agent and other solutions back to the solution pool agent. The algorithm agent also sends a search report to the adviser agent."

     > "After timeout the problem agent sends `done` message to all agents"
